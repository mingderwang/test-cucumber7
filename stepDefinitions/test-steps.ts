import { When, Then, Given } from "@cucumber/cucumber";
//import { CustomWorld } from "../world";
//import { SimpleOne } from "../domains/eating/simple-one";
//import expect from "expect";

Given("Alice apply a new account", async function () {
  // Write code here that turns the phrase above into concrete actions
  return "pending";
});

When("she has a new account", async function () {
  // Write code here that turns the phrase above into concrete actions
  return "pending";
});

Then("her account balance is {int} Ether", async function (int) {
  // Then('her account balance is {float} Ether', async function (float) {
  // Write code here that turns the phrase above into concrete actions
  console.log(int);
  return "pending";
});

When("Alice receive {int} Ethers from a faucet", async function (int) {
  // When('Alice receive {float} Ethers from a faucet', async function (float) {
  // Write code here that turns the phrase above into concrete actions
  console.log(int);
  return "pending";
});

Then("her account balance is {int} Ethers", async function (int) {
  // Then('her account balance is {float} Ethers', async function (float) {
  // Write code here that turns the phrase above into concrete actions
  console.log(int);
  return "pending";
});
